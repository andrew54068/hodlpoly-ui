export default function getHeatMapColors(allLandPrices: number[]) {
  console.log('allLandPrices :', allLandPrices);

  // const maxVal = Math.max(...allLandPrices);
  // const grade = Math.ceil(maxVal / 5);
  // console.log('grade :', grade);
}