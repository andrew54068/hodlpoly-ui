export enum NumberType { Any, Odd, Even, Low, High }

export enum PropsType {
    OddDice = 0,
    EvenDice = 1,
    LowDice = 2,
    HighDice = 3,
    TitleDeed = 4,
    Ticket = 5, // 10 tickets for 1 lottery ticket
    LotteryTicket = 6,
    WorldWideTravel = 7,
}

export enum SelectingLandPurpose {
    LandFlipper,
    WorldWideTravel
}